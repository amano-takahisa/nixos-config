{ ... }:

{
  programs.nixvim.plugins.comment = {
    enable = false;
    settings = {
      padding = true;
      sticky = true;
      ignore = "^$";
      toggler = {
        line = "gcc";
        block = "gbc";
      };
      opleader = {
        line = "gc";
        block = "gb";
      };
      extra = {
        above = "gcO";
        below = "gco";
        eol = "gcA";
      };
      mappings = {
        basic = true;
        extra = true;
      };
    };
  };
}
