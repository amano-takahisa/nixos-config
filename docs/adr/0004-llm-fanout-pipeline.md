# ADR-0004: LLM並列実装パイプライン（fanout）の採用

## ステータス

提案中（2026-07-17）

## コンテキストと問題

Claude Codeでの実装作業を、大きいモデル（親: Fable/Opus等）が要求を実装計画に落とし込み、
軽量モデル（実装子: Haiku）が並行してタスクを実装し、独立したレビュー用モデル（Sonnet）が
PRをレビューする体制にしたい。親モデルとの対話でまとめた要求は既に `grill-me` → `adr` → `plan`
の流れで実装計画（`docs/plans/`）に落とし込めるが、この計画は現状 `next` スキルによる
1タスクずつの逐次消化のみを前提にしており、複数タスクを安全に並列実装させる仕組みがない。

サブエージェントは親の会話履歴を一切参照できず、委譲時のプロンプト文字列のみが情報伝達路になる。
また、同一チェックアウト上で複数エージェントを走らせるとファイル編集が衝突する。この2点を
解決しないまま並列化すると、実装漏れやマージ地獄が起きる。

## 決定ドライバー

- 自前でオーケストレーション機構を組まず、Claude Code既存機能（サブエージェント定義、
  `isolation: worktree`、モデル指定）を最大限活用する
- 軽量モデルは曖昧さに弱いため、タスク仕様は自己完結したファイルとして渡す
- 共有部分（型・スキーマ等）を確定してからファンアウトしないとマージ地獄になる
- 並列実装の安全性（ファイル衝突の回避）と、計画書への書き戻しの一貫性を両立する

## 検討した選択肢

- 自前でオーケストレーション機構（プロセス管理・tmuxペイン制御等）をゼロから構築する
- Claude Codeのサブエージェント機構（`.claude/agents/`）+ `isolation: worktree` +
  既存の `plan`/`next` スキル資産を拡張して構築する
- Agent Teams（共有タスクリスト・エージェント間メッセージングによる密な協調）を最初から採用する

## 決定内容

選択した選択肢: "Claude Codeのサブエージェント機構 + isolation: worktree + 既存skill資産の拡張"

まず1セッション内のサブエージェント並列（`isolation: worktree`）から始め、tmuxによる複数セッション
監視やAgent Teamsは、ボトルネックが顕在化してから検討する次段階と位置づける。具体的な構成:

1. **plan の拡張**: タスクテンプレートに「変更ファイル」（glob表記。参照のみの共有ファイルは
   `（読み取りのみ）` と注記）と「依存」（依存タスクID）を追加する。並列可否は
   「依存タスクが全て完了」かつ「書き込み対象globが互いに素（読み取り専用は無視）」で判定する。
2. **fanout スキルの新設**（`next` の並列版）: 条件を満たすタスク群を抽出し、各タスクについて
   ADR・計画書から自己完結した仕様ファイルを `docs/plans/NNNN-tasks/<タスク番号>.md` に生成する。
   このファイルは **worktree作成前に現在の作業ブランチ（main）へ直接コミット**する
   （`git worktree` はHEADのコミット状態から作られ、未コミットの変更を引き継がないため）。
   実行前に並列実行するタスク一覧をユーザーに提示し承認を得る。
3. **エージェント定義**: `.claude/agents/implementer.md`（model: haiku, tools: Read/Write/Edit/
   Bash/Glob/Grep, isolation: worktree）と `.claude/agents/reviewer.md`（model: sonnet, tools:
   Read/Grep/Glob/Bash のみ、Write/Edit権限なし）を、既存の `skills/` と同じ要領で
   `modules/home-manager/misc/claude-code/agents/` 配下に置き、home-managerの `home.file` で
   `.claude/agents/` にシンボリックリンクして宣言的に管理する。
4. **ブランチ/マージ戦略**: implementerはworktree内で `task/NNNN-X` ブランチにコミットする。
   マージ条件・自動化の是非は [ADR-0005](0005-fanout-auto-merge-without-human-approval.md) に分離する。
   承認された場合、親が `git merge --no-ff` でmainに取り込み、worktreeとブランチを削除する。
5. **エスカレーション規則**: implementerが同一タスクで2回失敗したらリトライを止めて親に
   エスカレーションする。reviewerの差し戻しは1往復までとし、差し戻しは新規エージェントではなく
   同一implementerインスタンスをSendMessageで文脈ごと再開して行う。2回目のNGも親にエスカレーション
   する。
6. **並列数**: implementerの並列実行数に固定上限は設けず、毎回親がタスク量や使用量上限を見て判断する。

### 良い結果

- 自前のプロセス管理・tmux制御を実装せずに済み、Claude Code本体の更新に追従しやすい
- ファイル所有権ベースのタスク分割により、worktree間の編集衝突が構造的に起きない
- タスク仕様ファイルが監査証跡としてリポジトリに残る
- 既存の `plan`/`next`/`adr`/`commit` スキルとの一貫性を保ったまま拡張できる

### 悪い結果

- タスク分割の質（ファイル所有権の切り方）が並列化の成否を左右し、分割を誤ると並列度が上がらない
- 仕様ファイルをworktree作成前にmainへ直接コミットする必要があり、fanout実行のたびにmainへの
  コミットが増える（計画書本体とは別に `docs/plans/NNNN-tasks/` 配下が肥大化する）
- Agent Teamsのような密な協調（動的なタスク再割り当て等）はスコープ外とし、当面は静的な
  事前分割に依存する

## 参考

- [ADR-0005: reviewer承認+機械的ゲートのみでの自動マージ（人間承認なし）](0005-fanout-auto-merge-without-human-approval.md)
- 元になった壁打ち: Claude共有会話（並列実装パイプラインの構想と技術検証）
